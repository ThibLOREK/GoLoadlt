APP_NAME=go-etl-studio
APP_ENV=development
HTTP_PORT=8080
# Laisser vide en dev local pour utiliser le store en mémoire (pas de PostgreSQL requis)
# En production, renseigner le DSN complet
POSTGRES_DSN=
REDIS_ADDR=localhost:6379
JWT_SECRET=change-me
FRONTEND_ORIGIN=http://localhost:3000
